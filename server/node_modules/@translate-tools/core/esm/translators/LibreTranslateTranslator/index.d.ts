import { langCode, langCodeWithAuto } from '../../types/Translator';
import { BaseTranslator, TranslatorOptions } from '../../util/BaseTranslator';
export type LibreTranslateTranslatorOptions = {
    apiEndpoint?: string;
};
export declare class LibreTranslateTranslator extends BaseTranslator<LibreTranslateTranslatorOptions> {
    static readonly translatorName = "LibreTranslateTranslator";
    static isRequiredKey: () => boolean;
    static isSupportedAutoFrom: () => boolean;
    static getSupportedLanguages(): langCode[];
    private readonly apiEndpoint;
    constructor(options: TranslatorOptions<LibreTranslateTranslatorOptions>);
    getLengthLimit(): number;
    getRequestsTimeout(): number;
    checkLimitExceeding(text: string | string[]): number;
    translate(text: string, from: langCodeWithAuto, to: langCode): Promise<string>;
    translateBatch(texts: string[], from: langCodeWithAuto, to: langCode): Promise<string[]>;
}
